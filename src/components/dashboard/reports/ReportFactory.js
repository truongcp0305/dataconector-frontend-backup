import { getDefaultReportConfig } from '@/components/dashboard/configPool/defaultReportConfig';

getDefaultReportConfig;
/**
 * Class phục phục cho việc tạo lại các report
 */
class ReportFactory {
    static getDefaultConfig(reportType, symperId = '') {}
}
