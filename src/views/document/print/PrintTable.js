export default class PrintTable {
    constructor(control, tableName, keyInstance) {
        this.tableControl = control;
        this.tableName = tableName;
        this.keyInstance = keyInstance;
    }
    render() {
        console.log('ádsadsad', this.ele);
    }
}
