const defaultState = {
    flagGetPermissionPack: true,
    flagGetActionPack: true,
    allPermissionPack: {}, // tất cả các permission pack có dạng {id : pack},
    allActionPack: {}, // tất cả các action pack, dạng {id: pack}
};

export default defaultState;
