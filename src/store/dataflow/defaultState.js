const defaultState = {
    allDataflow: {
        // lưu lại tất cả các config của các dataflow theo instanceKey, cấu hình mặc định trong file src\components\dataflow\configPool\dataflowConfig.js
    },
};

export default defaultState;
